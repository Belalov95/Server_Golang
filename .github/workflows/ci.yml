name: Go CI

on:
  push:
    branches: [ "db" ]
  pull_request:
    branches: [ "db" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23.2'

    - name: Install dependencies
      run: go mod download

    - name: Add gomock to dependencies
      run: go get github.com/golang/mock/mockgen@v1.6.0

    - name: Install mockgen
      run: go install github.com/golang/mock/mockgen@v1.6.0


    - name: Generate mocks
      run: |
        mockgen -source=internal/repository/interface.go -destination=internal/mocks/goods_mock.go -package=mocks

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
